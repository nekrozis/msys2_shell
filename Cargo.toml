[package]
name = "msys2_shell_rs"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
ctrlc = "3.5"
lexopt = "0.3"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
strip = "debuginfo"
lto = "fat"
panic = "abort"
codegen-units = 1
